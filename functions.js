function showLeaderboard() {
    // Lógica para mostrar la tabla de clasificación (implementación necesaria)
    alert("Mostrar Tabla de Clasificación: Implementación necesaria");
}

function shareScore() {
    const score = document.getElementById("final-score-value").textContent;
    const shareUrl = "https://tu-sitio.com"; // Reemplaza con la URL de tu juego
    const socialMessage = `¡Acabo de obtener ${score} puntos en el Juego Marxiano - Capital Quiz! #JuegoMarxiano #CapitalQuiz`;

    // Lógica para compartir en redes sociales (implementación necesaria)
    alert(`Compartir Puntaje: Implementación necesaria\nMensaje: ${socialMessage}\nURL: ${shareUrl}`);
}

function showAchievements() {
    // Lógica para mostrar logros (implementación necesaria)
    alert("Mostrar Logros: Implementación necesaria");
}

function startMultiplayerMode() {
    // Lógica para iniciar el modo multijugador (implementación necesaria)
    alert("Modo Multijugador: Implementación necesaria");
}
